import './style.css'; // Importa tus estilos CSS

// Este archivo se usa para inicializar lógica global o para inyectar scripts en las páginas
// que no tienen un .ts dedicado.

// Dado que usas un archivo .ts por cada página (registro.ts, login.ts),
// este archivo puede permanecer vacío o solo importar estilos.

console.log("Aplicación FoodStore inicializada.");

// Si usas un sistema de ruteo más complejo, aquí iría la lógica de renderizado.
// Para tu estructura actual (HTML + TS por página), solo es un punto de inicio.